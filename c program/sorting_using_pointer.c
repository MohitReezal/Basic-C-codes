#include<stdio.h>
#include<string.h>
int main(){
int *p;
int n;
char a[20];
printf("Enter the number of strings");
scanf("%d",&n);
printf("Enter the strings to be sorted");
for(int i=0;i<=n;i++){
    scanf("%s",a[i]);
}

}